add_library(
    meccha_application
    STATIC
        src/application_snapshot.cpp
        src/application_root.cpp
        src/callback_barrier.cpp
        src/config_codec.cpp
        src/config_store.cpp
        src/game_thread_scheduler.cpp
        src/job_state.cpp
        src/localization.cpp
        src/mesh_profile_codec.cpp
        src/runtime_lifecycle.cpp
        src/runtime_operation_executor.cpp
)
add_library(MecchaCamouflage::Application ALIAS meccha_application)

target_include_directories(
    meccha_application
    PUBLIC
        "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/include>"
)
target_link_libraries(
    meccha_application
    PUBLIC
        MecchaCamouflage::Core
    PRIVATE
        glaze::glaze
        meccha_build_options
)

if(WIN32)
    target_sources(
        meccha_application
        PRIVATE
            src/config_storage_win32.cpp
    )
    target_link_libraries(
        meccha_application
        PRIVATE
            ole32
            shell32
    )
endif()
